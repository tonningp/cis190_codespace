# Working with Files the **mv** command 

The mv command is used to rename or move files. The

- **mv**: Rename or move files

> **Warning**: `cp` and `mv` can overwrite files; use `-i` for interactive mode.

