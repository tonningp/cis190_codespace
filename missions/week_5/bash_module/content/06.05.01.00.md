## Redirection and Pipes

* Output redirection:

  * `>` to overwrite
  * `>>` to append
* Input redirection:

  * `< file` uses contents of a file as input
* Pipes:

  * `|` sends output of one command to another

